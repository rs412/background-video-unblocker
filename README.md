# background_video_unblocker
Background Video Unblocker 是基于 Manifest V3 标准的轻量浏览器扩展（Chrome/Edge 通用）。 拦截网站用于检测切页、失焦的 `visibilitychange`、`blur`、`pagehide` 等监听事件，伪造页面永久可见、窗口永久聚焦状态，自动恢复被暂停的视频，实现切换标签、最小化窗口、鼠标移出浏览器时视频不会暂停。
